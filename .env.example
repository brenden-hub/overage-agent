ANTHROPIC_API_KEY=
OVERAGE_MODEL=claude-opus-4-7

HUBSPOT_TOKEN=
HUBSPOT_PORTAL_ID=22007177

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/REPLACE/ME/HERE

# Set to "false" only when you are ready to actually post to #revenue_overages.
# Default true => no Slack posts, code logs the payload instead.
DRY_RUN=true

# Optional: only consider accounts whose usage > limit by this fraction (0.0 = any overage).
OVERAGE_THRESHOLD=0.0
